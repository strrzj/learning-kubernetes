apiVersion: v1
kind: Service
metadata:
  name: openelb-admission
  namespace: openelb-system
spec:
  ports:
  - name: https-webhook
    port: 443
    targetPort: webhook
  selector:
    app: openelb-manager
    control-plane: openelb-manager
  type: ClusterIP
