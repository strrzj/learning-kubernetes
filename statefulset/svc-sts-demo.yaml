apiVersion: v1
kind: Service
metadata:
  name: svc-sts-demo
  namespace: ns-demo
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: demoapp
    controller: sts-demo
  type: ClusterIP
